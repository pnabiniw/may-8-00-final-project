"""
What is ORM?
"""

# ORM => Object Relational Mapping
# Django provides an interface to query database which is called as ORM
# sqlalchemy

# Django ORM Student.objects.get(id=id)
# SQLAlchemy => session.query(Student).filter(Student.id==id).first()
